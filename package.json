{
  "dependencies": {
    "bootstrap": "^5.3.3",
    "prettier": "3.2.5"
  },
  "scripts": {
    "format": "npx prettier ./src --write && docker run -v $(pwd):/code ghcr.io/php-cs-fixer/php-cs-fixer:${FIXER_VERSION:-3-php8.3} fix ./src"
  },
  "prettier": {
    "semi": false,
    "singleQuote": true,
    "tabWidth": 2,
    "printWidth": 80
  }
}
